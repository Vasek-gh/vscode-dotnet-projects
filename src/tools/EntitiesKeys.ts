export class EntitiesKeys {
    private static readonly dotnet = "dotnet";
    public static readonly templates = `${this.dotnet}.templates`;
    public static readonly favoritesTemplates = `${this.dotnet}.favorites-templates`;
    public static readonly activeSolution = `${this.dotnet}.active-solution`;
    public static readonly autoSolutionPrefix = `${this.dotnet}.auto-solution-prefix`;
}