export class EntitiesKeys {
    private static readonly group = "dotprojs";

    public static readonly templates = `${this.group}.templates`;
    public static readonly favoritesTemplates = `${this.group}.favorites-templates`;
    public static readonly activeSolution = `${this.group}.active-solution`;
    public static readonly autoSolutionPrefix = `${this.group}.auto-solution-prefix`;
    public static readonly solutionFormat = `${this.group}.solution-format`;
}